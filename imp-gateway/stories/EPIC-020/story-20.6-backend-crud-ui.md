# Story 20.6: Backend CRUD UI 구현

## 개요

| 항목 | 내용 |
|------|------|
| **Story ID** | 20.6 |
| **Epic** | EPIC-020 (Route & Policy 설정) |
| **우선순위** | P0 |
| **예상 공수** | 1.5일 |
| **상태** | 🔲 미시작 |
| **담당자** | - |

## 목표

Backend의 생성, 조회, 수정, 삭제 기능을 위한 완전한 UI를 구현한다. Envoy Gateway Backend의 모든 설정(Endpoints, Load Balancer, Health Check, Circuit Breaker, TLS)을 지원한다.

## 의존성

- Story 20.2 (Backend 엔티티)
- Story 20.4 (API Service 상세 탭)

## 요구사항

### 기능 요구사항

#### 1. Backend 목록 (`widgets/provider/service-backends-table/`)

- **테이블 컬럼**
  - Name: Backend 이름 (클릭 시 상세로 이동)
  - Scheme: http/https/grpc/grpcs 배지
  - Endpoints: 엔드포인트 수 표시
  - LB Policy: Round Robin/Least Request 등
  - Status: Active/Inactive 배지
  - Actions: Edit, Delete 버튼

- **기능**
  - 정렬: Name, Created 기준
  - 필터: Status (All/Active/Inactive)
  - 검색: Name으로 검색

#### 2. Backend 생성 폼 (`features/backend/create-backend/`)

**기본 정보**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Basic Information                                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Name *                                                                  │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ user-api-backend                                                 │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│ Only lowercase letters, numbers, and hyphens allowed                    │
│                                                                         │
│ Scheme *                                                                │
│ ○ HTTP    ● HTTPS    ○ gRPC    ○ gRPCs                                 │
│                                                                         │
│ Status                                                                  │
│ [✓] Active                                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Endpoints (필수, 동적 목록)**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Endpoints *                                          [+ Add Endpoint]   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Host *              Port *     Weight    Zone (optional)         │    │
│ │ ┌─────────────────┐ ┌──────┐  ┌──────┐  ┌─────────────────┐     │    │
│ │ │ api.example.com │ │ 443  │  │ 50   │  │ us-east-1       │ [×] │    │
│ │ └─────────────────┘ └──────┘  └──────┘  └─────────────────┘     │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ ┌─────────────────┐ ┌──────┐  ┌──────┐  ┌─────────────────┐     │    │
│ │ │ api2.example.com│ │ 443  │  │ 50   │  │ us-west-2       │ [×] │    │
│ │ └─────────────────┘ └──────┘  └──────┘  └─────────────────┘     │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ℹ️ At least one endpoint is required. Weight sum should equal 100.     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Load Balancing**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Load Balancing                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Algorithm                                                               │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ [▼ Round Robin]                                                  │    │
│ │   ├─ Round Robin (default)                                       │    │
│ │   ├─ Least Request                                               │    │
│ │   ├─ Random                                                      │    │
│ │   └─ Consistent Hash                                             │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ▼ Consistent Hash Settings (shown when Consistent Hash selected)       │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Hash Type: ○ Source IP  ○ Header  ○ Cookie                       │    │
│ │                                                                  │    │
│ │ Header Name: [X-User-ID        ] (when Header selected)         │    │
│ │                                                                  │    │
│ │ Cookie Name: [session          ] (when Cookie selected)         │    │
│ │ Cookie TTL:  [3600     ] seconds                                │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Connection Settings**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Connection Settings                                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Connect Timeout        Request Timeout        Idle Timeout              │
│ ┌──────────┐ ms       ┌──────────┐ ms       ┌──────────┐ ms            │
│ │ 10000    │          │ 30000    │          │ 60000    │               │
│ └──────────┘          └──────────┘          └──────────┘               │
│                                                                         │
│ ℹ️ Connect: time to establish connection                                │
│    Request: total time for request/response                             │
│    Idle: time before closing idle connection                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Health Check (선택)**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Health Check                                          [✓] Enable        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Type: ○ HTTP  ○ TCP  ○ gRPC                                            │
│                                                                         │
│ ▼ HTTP Health Check Settings                                            │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Path *          Interval        Timeout                          │    │
│ │ ┌─────────────┐ ┌─────────┐ s   ┌─────────┐ s                   │    │
│ │ │ /health     │ │ 10      │     │ 5       │                     │    │
│ │ └─────────────┘ └─────────┘     └─────────┘                     │    │
│ │                                                                  │    │
│ │ Healthy Threshold    Unhealthy Threshold                         │    │
│ │ ┌─────────┐          ┌─────────┐                                │    │
│ │ │ 2       │          │ 3       │                                │    │
│ │ └─────────┘          └─────────┘                                │    │
│ │                                                                  │    │
│ │ Expected Status Codes (optional)                                 │    │
│ │ ┌─────────────────────────────────────────────────────────┐     │    │
│ │ │ 200, 201, 204                                           │     │    │
│ │ └─────────────────────────────────────────────────────────┘     │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Circuit Breaker (선택)**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Circuit Breaker                                       [✓] Enable        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Max Connections        Max Pending Requests                             │
│ ┌──────────┐          ┌──────────┐                                     │
│ │ 1024     │          │ 1024     │                                     │
│ └──────────┘          └──────────┘                                     │
│                                                                         │
│ Max Requests           Max Retries                                      │
│ ┌──────────┐          ┌──────────┐                                     │
│ │ 1024     │          │ 3        │                                     │
│ └──────────┘          └──────────┘                                     │
│                                                                         │
│ ℹ️ Circuit breaker prevents cascading failures by limiting             │
│    concurrent requests to unhealthy backends.                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Retry Policy (선택)**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ Retry Policy                                          [✓] Enable        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Number of Retries      Per Try Timeout                                  │
│ ┌──────────┐          ┌──────────┐ seconds                             │
│ │ 3        │          │ 10       │                                     │
│ └──────────┘          └──────────┘                                     │
│                                                                         │
│ Retry On (select conditions)                                            │
│ [✓] 5xx           Server errors                                         │
│ [✓] reset         Connection reset                                      │
│ [✓] connect-failure Connection failure                                  │
│ [ ] retriable-4xx  Retriable 4xx errors                                 │
│ [ ] refused-stream Stream refused                                       │
│                                                                         │
│ Backoff (optional)                                                      │
│ Base Interval: [25    ] ms    Max Interval: [250   ] ms                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**TLS Settings (선택, HTTPS/gRPCs일 때 표시)**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ TLS Settings                                                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ SNI (Server Name Indication)                                            │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ api.example.com                                                  │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│ ℹ️ Override the hostname used in TLS handshake                         │
│                                                                         │
│ [ ] Skip Certificate Verification (insecure)                            │
│ ⚠️ Only use for development/testing environments                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3. Backend 수정 폼 (`features/backend/update-backend/`)
- 생성 폼과 동일한 필드
- 기존 값으로 초기화

#### 4. Backend 삭제 다이얼로그 (`features/backend/delete-backend/`)
```
┌─────────────────────────────────────────────────────────────────────────┐
│ ⚠️ Delete Backend                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Are you sure you want to delete backend "user-api-backend"?             │
│                                                                         │
│ Configuration:                                                          │
│ • Scheme: HTTPS                                                         │
│ • Endpoints: 2                                                          │
│ • LB Policy: Round Robin                                                │
│                                                                         │
│ ⚠️ This backend is referenced by 3 routes:                              │
│ • get-users                                                             │
│ • create-user                                                           │
│ • update-user                                                           │
│                                                                         │
│ Deleting this backend will disconnect these routes from any backend.    │
│                                                                         │
│                                    [Cancel] [Delete Backend]            │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 5. Backend 상세 페이지 (`pages/provider/backend-detail-page.tsx`)
```
┌─────────────────────────────────────────────────────────────────────────┐
│ ← Back to Backends                                                      │
│                                                                         │
│ Backend: user-api-backend                          [Edit] [Delete]      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Status: ● Active       Scheme: HTTPS       Created: 2024-01-15         │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Endpoints                                                        │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │                                                                  │    │
│ │ ┌───────────────────────────────────────────────────────────┐   │    │
│ │ │ api-1.example.com:443    Weight: 50%    Zone: us-east-1   │   │    │
│ │ │ ● Healthy (last check: 5s ago)                             │   │    │
│ │ └───────────────────────────────────────────────────────────┘   │    │
│ │ ┌───────────────────────────────────────────────────────────┐   │    │
│ │ │ api-2.example.com:443    Weight: 50%    Zone: us-west-2   │   │    │
│ │ │ ● Healthy (last check: 3s ago)                             │   │    │
│ │ └───────────────────────────────────────────────────────────┘   │    │
│ │                                                                  │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Load Balancing                                                   │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ Algorithm: Round Robin                                           │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Health Check                                        ● Enabled    │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ Type: HTTP    Path: /health    Interval: 10s                    │    │
│ │ Healthy threshold: 2    Unhealthy threshold: 3                  │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Connection Settings                                              │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ Connect: 10s    Request: 30s    Idle: 60s                       │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Circuit Breaker                                     ● Enabled    │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ Max Connections: 1024    Max Pending: 1024    Max Retries: 3    │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Retry Policy                                        ● Enabled    │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ Retries: 3    Per Try: 10s    On: 5xx, reset, connect-failure   │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────┐    │
│ │ Used by Routes                                                   │    │
│ │ ─────────────────────────────────────────────────────────────── │    │
│ │ • get-users (/api/v1/users, GET)                                │    │
│ │ • create-user (/api/v1/users, POST)                             │    │
│ │ • update-user (/api/v1/users/:id, PUT)                          │    │
│ └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 비기능 요구사항

- Zod 스키마로 유효성 검증
- Endpoints는 최소 1개 필수
- Port 범위 검증 (1-65535)
- Endpoint Weight 합계 100 검증 (선택적)
- 폼 제출 중 로딩 상태 표시
- 성공/실패 toast 알림
- 삭제 시 연결된 Route 경고

## 기술 상세

### Backend 데이터 모델

```typescript
interface Backend {
  id: string;
  api_service_id: string;
  name: string;

  // Connection
  scheme: 'http' | 'https' | 'grpc' | 'grpcs';
  endpoints: BackendEndpoint[];

  // Load Balancing
  lb_policy: 'RoundRobin' | 'LeastRequest' | 'Random' | 'ConsistentHash';
  consistent_hash?: {
    type: 'SourceIP' | 'Header' | 'Cookie';
    header_name?: string;
    cookie?: { name: string; ttl?: string };
  };

  // Timeouts (milliseconds)
  connect_timeout_ms?: number;
  timeout_ms?: number;
  idle_timeout_ms?: number;

  // Health Check
  health_check?: {
    type: 'HTTP' | 'TCP' | 'GRPC';
    interval: string;
    timeout: string;
    unhealthy_threshold: number;
    healthy_threshold: number;
    http?: {
      path: string;
      expected_statuses?: number[];
    };
  };

  // Circuit Breaker
  circuit_breaker?: {
    max_connections?: number;
    max_pending_requests?: number;
    max_requests?: number;
    max_retries?: number;
  };

  // Retry
  retry?: {
    num_retries: number;
    retry_on: string[];
    per_try_timeout?: string;
    backoff?: {
      base_interval: string;
      max_interval: string;
    };
  };

  // TLS
  tls?: {
    sni?: string;
    insecure_skip_verify?: boolean;
  };

  status: 'active' | 'inactive';
  created_at: string;
  updated_at: string;
}

interface BackendEndpoint {
  host: string;
  port: number;
  weight?: number;
  zone?: string;
}
```

### Zod 스키마

```typescript
const backendEndpointSchema = z.object({
  host: z.string().min(1, 'Host is required'),
  port: z.number().int().min(1).max(65535, 'Port must be between 1 and 65535'),
  weight: z.number().int().min(0).max(100).optional(),
  zone: z.string().optional(),
});

const healthCheckSchema = z.object({
  type: z.enum(['HTTP', 'TCP', 'GRPC']),
  interval: z.string().default('10s'),
  timeout: z.string().default('5s'),
  unhealthy_threshold: z.number().int().min(1).default(3),
  healthy_threshold: z.number().int().min(1).default(2),
  http: z.object({
    path: z.string().min(1),
    expected_statuses: z.array(z.number()).optional(),
  }).optional(),
});

const circuitBreakerSchema = z.object({
  max_connections: z.number().int().min(1).optional(),
  max_pending_requests: z.number().int().min(1).optional(),
  max_requests: z.number().int().min(1).optional(),
  max_retries: z.number().int().min(0).optional(),
});

const retrySchema = z.object({
  num_retries: z.number().int().min(1).default(3),
  retry_on: z.array(z.string()).default(['5xx', 'reset', 'connect-failure']),
  per_try_timeout: z.string().optional(),
  backoff: z.object({
    base_interval: z.string(),
    max_interval: z.string(),
  }).optional(),
});

const createBackendSchema = z.object({
  name: z.string()
    .min(1, 'Name is required')
    .regex(/^[a-z0-9-]+$/, 'Only lowercase letters, numbers, and hyphens allowed'),

  scheme: z.enum(['http', 'https', 'grpc', 'grpcs']),
  endpoints: z.array(backendEndpointSchema).min(1, 'At least one endpoint is required'),

  lb_policy: z.enum(['RoundRobin', 'LeastRequest', 'Random', 'ConsistentHash']).default('RoundRobin'),
  consistent_hash: z.object({
    type: z.enum(['SourceIP', 'Header', 'Cookie']),
    header_name: z.string().optional(),
    cookie: z.object({
      name: z.string(),
      ttl: z.string().optional(),
    }).optional(),
  }).optional(),

  connect_timeout_ms: z.number().int().min(0).optional(),
  timeout_ms: z.number().int().min(0).optional(),
  idle_timeout_ms: z.number().int().min(0).optional(),

  health_check: healthCheckSchema.optional(),
  circuit_breaker: circuitBreakerSchema.optional(),
  retry: retrySchema.optional(),

  tls: z.object({
    sni: z.string().optional(),
    insecure_skip_verify: z.boolean().optional(),
  }).optional(),

  status: z.enum(['active', 'inactive']).default('active'),
});
```

### FSD 구조

```
web/src/
├── entities/backend/
│   ├── model/types.ts           # Backend 타입 정의
│   ├── api/backend-api.ts       # API 훅
│   └── ui/
│       ├── backend-status-badge.tsx
│       ├── backend-scheme-tag.tsx
│       ├── backend-endpoints-count.tsx
│       └── backend-health-indicator.tsx
│
├── features/backend/
│   ├── create-backend/
│   │   ├── model/schema.ts      # Zod 스키마
│   │   ├── ui/
│   │   │   ├── create-backend-form.tsx
│   │   │   ├── endpoints-editor.tsx
│   │   │   ├── lb-policy-selector.tsx
│   │   │   ├── consistent-hash-settings.tsx
│   │   │   ├── connection-settings.tsx
│   │   │   ├── health-check-settings.tsx
│   │   │   ├── circuit-breaker-settings.tsx
│   │   │   ├── retry-settings.tsx
│   │   │   └── tls-settings.tsx
│   │   └── index.ts
│   ├── update-backend/
│   │   ├── ui/
│   │   │   └── update-backend-form.tsx
│   │   └── index.ts
│   ├── delete-backend/
│   │   ├── ui/
│   │   │   └── delete-backend-dialog.tsx
│   │   └── index.ts
│   └── index.ts
│
├── widgets/provider/
│   └── service-backends-table/
│       ├── ui/
│       │   ├── backends-table.tsx
│       │   ├── backends-table-row.tsx
│       │   └── backends-table-filters.tsx
│       └── index.ts
│
└── pages/provider/
    ├── backend-detail-page.tsx
    └── backends-page.tsx        # Standalone (선택)
```

### App Router

```
web/app/provider/services/[id]/backends/
├── new/page.tsx              -> Backend 생성 페이지
└── [backendId]/
    ├── page.tsx              -> Backend 상세 페이지
    └── edit/page.tsx         -> Backend 수정 페이지
```

## 수용 기준

### Backend 목록
- [ ] Backend 목록이 테이블로 표시된다
- [ ] Name, Scheme, Endpoints, LB Policy, Status 컬럼이 표시된다
- [ ] Status로 필터링할 수 있다
- [ ] Name으로 검색할 수 있다

### Backend 생성
- [ ] Backend 생성 폼이 동작한다
- [ ] Scheme 선택이 동작한다
- [ ] Endpoints를 동적으로 추가/삭제할 수 있다
- [ ] LB Policy 선택이 동작한다
- [ ] Consistent Hash 설정이 동작한다 (선택 시)
- [ ] Connection Settings 입력이 동작한다
- [ ] Health Check 설정이 동작한다
- [ ] Circuit Breaker 설정이 동작한다
- [ ] Retry Policy 설정이 동작한다
- [ ] TLS 설정이 동작한다 (HTTPS/gRPCs 선택 시)
- [ ] 유효성 검증이 동작한다

### Backend 수정
- [ ] Backend 수정 폼이 기존 값으로 초기화된다
- [ ] 모든 필드 수정이 가능하다

### Backend 삭제
- [ ] 삭제 확인 다이얼로그가 표시된다
- [ ] 연결된 Route 경고가 표시된다

### Backend 상세
- [ ] Backend 상세 페이지가 동작한다
- [ ] Endpoints가 Health 상태와 함께 표시된다
- [ ] 모든 설정 섹션이 표시된다
- [ ] 연결된 Routes가 표시된다

## 참조

- Envoy Gateway Backend: https://gateway.envoyproxy.io/docs/api/extension_types/#backend
- 패턴 참조: `web/src/features/service/create-service/`
- 동적 목록: `web/src/features/gateway/create/` (Listeners)
