# STORY-16.2: API Services List Page

## 1. 개요
**Epic**: EPIC-016 API Service 관리
**제목**: API Service 목록 페이지 구현
**담당자**: AI Agent
**상태**: 🔲 미시작

## 2. 목적
Provider Portal에서 API Service 목록을 테이블 형태로 조회하고, 검색/필터/페이지네이션을 지원하는 페이지를 구현한다.

## 3. 구현 상세

### 3.1. 라우트 구조
```
web/app/provider/services/
├── page.tsx                 # -> ServicesPage
└── new/
    └── page.tsx             # -> CreateServicePage (Story 16.3)
```

### 3.2. 페이지 컴포넌트
**Path**: `web/src/pages/provider/service/services-page.tsx`

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { Plus, Search, MoreHorizontal, Eye, Trash2 } from "lucide-react";

import { Button } from "@/shared/components/ui/button";
import { Input } from "@/shared/components/ui/input";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/shared/components/ui/select";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/shared/components/ui/table";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/shared/components/ui/dropdown-menu";
import { Skeleton } from "@/shared/components/ui/skeleton";
import { useServices, ServiceStatusBadge, type APIService } from "@/entities/service";
import { DeleteServiceDialog } from "@/features/service";

export function ServicesPage() {
  const [search, setSearch] = useState("");
  const [statusFilter, setStatusFilter] = useState<string>("all");
  const [deleteTarget, setDeleteTarget] = useState<APIService | null>(null);

  const { data, isLoading, error } = useServices({
    search: search || undefined,
    status: statusFilter !== "all" ? statusFilter : undefined,
  });

  // ... render
}
```

### 3.3. UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  API Services                                          [ + 새 API Service ] │
│  Provider의 API Service를 관리합니다                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────┐  ┌─────────────────┐                 │
│  │ 🔍 이름으로 검색...               │  │ 상태: All     ▼ │                 │
│  └──────────────────────────────────┘  └─────────────────┘                 │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ Name              │ Version │ Status   │ Created      │ Actions    │   │
│  ├───────────────────┼─────────┼──────────┼──────────────┼────────────┤   │
│  │ Payment API       │ 1.0.0   │ ● Active │ 2025-01-15   │    ⋮       │   │
│  │ User API          │ 2.1.0   │ ● Active │ 2025-01-10   │    ⋮       │   │
│  │ Order API         │ 1.0.0   │ ○ Inactive│ 2025-01-05  │    ⋮       │   │
│  └───────────────────┴─────────┴──────────┴──────────────┴────────────┘   │
│                                                                             │
│  Showing 1-3 of 3 services                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.4. 테이블 컬럼

| 컬럼 | 필드 | 설명 |
|------|------|------|
| Name | `name` | 클릭 시 상세 페이지로 이동 |
| Version | `version` | 버전 문자열 (없으면 "-") |
| Status | `status` | Active/Inactive 배지 |
| Created | `created_at` | 상대 시간 또는 날짜 |
| Actions | - | 드롭다운 메뉴 (View, Delete) |

### 3.5. 필터 기능

```tsx
// 상태 필터 옵션
const statusOptions = [
  { value: "all", label: "All" },
  { value: "active", label: "Active" },
  { value: "inactive", label: "Inactive" },
];
```

### 3.6. 빈 상태

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                            📦                                               │
│                                                                             │
│                    API Service가 없습니다                                   │
│           첫 번째 API Service를 생성하여 시작하세요                         │
│                                                                             │
│                      [ + 새 API Service 생성 ]                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 4. App Router 연결
**Path**: `web/app/provider/services/page.tsx`

```tsx
import { ServicesPage } from "@/pages/provider/service";

export default function Page() {
  return <ServicesPage />;
}
```

## 5. 수용 기준
- [ ] API Service 목록이 테이블로 표시된다.
- [ ] 이름으로 검색이 가능하다.
- [ ] 상태(Active/Inactive/All) 필터가 동작한다.
- [ ] "새 API Service" 버튼 클릭 시 생성 페이지로 이동한다.
- [ ] 행 클릭 또는 "View" 클릭 시 상세 페이지로 이동한다.
- [ ] "Delete" 클릭 시 삭제 확인 다이얼로그가 표시된다.
- [ ] 로딩 중 스켈레톤 UI가 표시된다.
- [ ] 에러 시 에러 메시지가 표시된다.
- [ ] 빈 상태 시 적절한 안내 UI가 표시된다.

## 6. 참조 파일
- `web/src/pages/operator/clusters-page.tsx` - 목록 페이지 패턴
- `web/src/pages/operator/agents-page.tsx` - 테이블 패턴

## 7. 비고
- 페이지네이션은 MVP에서 무한 스크롤 또는 "더 보기" 방식 검토
- 현재 백엔드에 `search`, `limit`, `offset` 파라미터 지원 여부 확인 필요
