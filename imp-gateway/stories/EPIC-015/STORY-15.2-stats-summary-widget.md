# STORY-15.2: Stats Summary Widget

## 1. 개요
**Epic**: EPIC-015 Fleet Dashboard
**제목**: 요약 카드 위젯
**담당자**: AI Agent
**상태**: ✅ 완료

## 2. 목적
전체 클러스터/에이전트 수와 상태별 개수를 한눈에 보여주는 요약 카드 위젯을 구현한다.

## 3. 현재 상태 (✅ 완료)

### 3.1. 컴포넌트 구조
**Path**: `web/src/widgets/operator/dashboard/stats-row.tsx`

```tsx
interface StatsRowProps {
  stats: OperatorDashboardStats | undefined;
  isLoading: boolean;
}

export function StatsRow({ stats, isLoading }: StatsRowProps)
```

### 3.2. 구현된 기능

| 카드 | 값 | 설명 | 링크 |
|------|---|------|------|
| Total Clusters | `total_clusters` | 활성 클러스터 수 표시 | `/operator/clusters` |
| Total Agents | `total_agents` | - | `/operator/agents` |
| Connected | `connected_agents` | "Online now" | - |
| Disconnected | `disconnected_agents` | "Currently offline" (위험 시 빨간색) | - |

### 3.3. UI 구현

```
┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
│ 🖥️ Total Clusters │ │ 🤖 Total Agents   │ │ ✅ Connected      │ │ ❌ Disconnected   │
│                   │ │                   │ │                   │ │                   │
│        3          │ │        8          │ │        6          │ │        2          │
│                   │ │                   │ │                   │ │                   │
│   2 active        │ │                   │ │   Online now      │ │  Currently offline│
└───────────────────┘ └───────────────────┘ └───────────────────┘ └───────────────────┘
```

### 3.4. 특징
- **클릭 가능**: Clusters/Agents 카드는 해당 목록 페이지로 링크
- **색상 변형**: Connected는 초록색, Disconnected는 빨간색 (값이 0보다 클 때)
- **로딩 상태**: Skeleton UI 표시
- **반응형**: 모바일(2열) / 데스크탑(4열)

## 4. EPIC 설계 대비

| 항목 | EPIC 설계 | 현재 구현 | 상태 |
|------|----------|----------|------|
| Clusters 요약 | O | O | ✅ |
| Agents 요약 | O | O | ✅ |
| Deployments 요약 | O | X | 🔲 Post-MVP |
| 상태별 색상 | O | O | ✅ |

> **참고**: Deployments 카드는 ProductPublish 기능 구현 후 추가 예정

## 5. 수용 기준
- [x] 전체 클러스터/에이전트 수가 표시된다.
- [x] 활성/비활성 상태가 구분되어 표시된다.
- [x] 클릭 시 해당 목록 페이지로 이동한다.
- [x] 로딩 중 스켈레톤 UI가 표시된다.
- [x] 문제가 있는 상태(Disconnected > 0)가 강조된다.

## 6. 참조 파일
- `web/src/widgets/operator/dashboard/stats-row.tsx`
- `web/src/widgets/operator/dashboard/index.ts`
- `web/src/pages/operator/dashboard-page.tsx`

## 7. 비고
Deployments 카드는 ProductPublish 엔티티 구현 후 추가 가능.
현재는 Cluster/Agent 중심의 Fleet 관리에 집중.
